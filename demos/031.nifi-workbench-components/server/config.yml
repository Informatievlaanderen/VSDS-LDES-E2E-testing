mongock:
  migration-scan-package: VSDS

springdoc:
  swagger-ui:
    path: /v1/swagger

management:
  tracing:
    enabled: false

ldes-server:
  host-name: http://${LDES_SERVER}:8080

spring:
  data:
    mongodb:
      uri: mongodb://ldes-mongodb:27017/ldes
      auto-index-creation: true
