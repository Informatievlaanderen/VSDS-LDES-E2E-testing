version: '3.3'
services:
  ldes-server-simulator:
    container_name: ldes-server-simulator
    env_file:
      - env.simulator
    image: ghcr.io/informatievlaanderen/ldes-server-simulator:${LDES_SERVER_SIMULATOR_TAG}
    environment:
      - BASEURL=http://ldes-server-simulator
      - SEED=/tmp/data
    ports:
      - ${LDES_SERVER_SIMULATOR_PORT}:80
    volumes:
      - type: bind
        source: ${LDES_SERVER_SIMULATOR_SEED_FOLDER}
        target: /tmp/data
    networks:
      - ldes
  nifi-workflow:
    container_name: nifi-workflow
    env_file:
      - env.workflow
    image: simulator-workflow-server-mongo/nifi-workflow
    build:
      context: ../../workflow
      args:
        PAT_READ_PACKAGES: ${PAT_READ_PACKAGES}
        LDES_CLIENT_NAR_TAG: ${LDES_CLIENT_NAR_TAG}
        SINGLE_USER_CREDENTIALS_USERNAME: ${SINGLE_USER_CREDENTIALS_USERNAME}
        SINGLE_USER_CREDENTIALS_PASSWORD: ${SINGLE_USER_CREDENTIALS_PASSWORD}
    ports:
      - ${NIFI_UI_PORT}:8443
    networks:
      - ldes
  ldes-server:
    container_name: ldes-server
    env_file:
      - env.server
    image: ghcr.io/informatievlaanderen/ldes-server:${LDES_SERVER_TAG}
    environment:
      - SPRING_DATA_MONGODB_HOST=ldes-mongodb
      - SPRING_DATA_MONGODB_PORT=27017
      - LDES_HOSTNAME=http://localhost:${LDES_SERVER_PORT}
    ports:
      - ${LDES_SERVER_PORT}:8080
    networks:
      - ldes
    depends_on:
      - ldes-mongodb
  ldes-mongodb:
    env_file:
      - env.mongo
    image: mongo:${MONGODB_TAG}
    container_name: ldes-mongodb
    ports:
      - ${MONGODB_PORT}:27017
    restart: always
    networks:
      - ldes
networks:
  ldes: {}
