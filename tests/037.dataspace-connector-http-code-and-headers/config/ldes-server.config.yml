springdoc:
  swagger-ui:
    path: /v1/swagger
management:
  tracing:
    enabled: false
ldes-server:
  host-name: http://localhost:8081
spring:
  datasource:
    url: jdbc:postgresql://ldes-postgres:5432/test
    username: admin
    password: admin
  batch:
    jdbc:
      initialize-schema: always


server:
  port: 8081
